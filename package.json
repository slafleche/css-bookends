{
  "name": "@css-bookends/monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "CSS-Bookends monorepo: typed, opt-in CSS helper books.",
  "license": "MIT",
  "author": "slafleche",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/slafleche/css-bookends.git"
  },
  "packageManager": "pnpm@11.0.9",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "//temp-exclude": "TEMP: color is excluded from lint/typecheck below pending its documented culori-rewrite gaps. Restore by deleting the `--filter='!...'` flags (and the matching set in lint-staged.config.mts). borders was restored once migrated onto self-publish.",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:browser": "playwright test",
    "lint": "pnpm --filter='!@css-bookends/color' -r lint",
    "typecheck": "pnpm --filter='!@css-bookends/color' -r run test:tsc",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "bash scripts/release-all.sh",
    "release:npm-only": "pnpm -r build && changeset publish",
    "prepare": "husky"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@playwright/test": "^1.61.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.7",
    "prettier": "^3.8.4",
    "prettier-plugin-jsdoc": "^1.8.0",
    "prettier-plugin-multiline-arrays": "^4.1.8"
  }
}
